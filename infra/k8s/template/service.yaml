apiVersion: v1
kind: Service
metadata:
  name: template-service
  namespace: carshare
spec:
  type: ClusterIP
  selector:
    app: template-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http