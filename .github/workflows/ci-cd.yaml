permissions:
  id-token: write
  contents: read
  
name: Deploy to AKS

on:
  push:
    branches: [ "main" ]


jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          cluster-name: my-aks
          resource-group: my-rg

      - name: Helm deploy
        run: |
          helm upgrade --install my-app ./chart \
            --namespace my-namespace \
            --create-namespace



# name: CI/CD Pipeline

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   build-and-test:
#     runs-on: ubuntu-latest
#     services:
#       postgres:
#         image: postgres:15
#         env:
#           POSTGRES_DB: brzina
#           POSTGRES_USER: brzina_user
#           POSTGRES_PASSWORD: changeme
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd "pg_isready -U brzina_user" --health-interval 10s --health-timeout 5s --health-retries 5
#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'
#       - name: Install Docker Buildx
#         uses: docker/setup-buildx-action@v3
#       - name: Build and test Python services
#         run: |
#           for svc in services/*; do
#             if [ -f "$svc/requirements.txt" ]; then
#               pip install -r "$svc/requirements.txt"
#               if [ -f "$svc/app/main.py" ]; then
#                 python -m compileall "$svc/app/main.py"
#               fi
#             fi
#           done
#       - name: Build Docker images
#         run: |
#           for svc in services/*; do
#             if [ -f "$svc/Dockerfile" ]; then
#               docker build -t brzina-$(basename $svc):latest $svc
#             fi
#           done
#           if [ -f frontend/Dockerfile ]; then
#             docker build -t brzina-frontend:latest frontend
#           fi
#       # Add steps for pushing images and deploying with Helm/Kubectl as needed
