replicaCount: 1
kafka:
  image: wurstmeister/kafka:latest
  pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 9092
zookeeper:
  image: wurstmeister/zookeeper:latest
  pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 2181
